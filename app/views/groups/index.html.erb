<h1>Listing groups</h1>
<br>
<table>
  <tr>
    <th>Name</th>
    <th>Owner</th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.name %></td>
    <td><%= group.owner_id %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group', new_group_path %>
